# Azure Configuration for Dynamic Service Availability
# Copy this file to .env and fill in your values

# Required: Azure Subscription ID
AZURE_SUBSCRIPTION_ID=your-subscription-id-here

# Authentication Option 1: Service Principal (recommended for production)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# Authentication Option 2: Use Azure CLI (for local development)
# Just run 'az login' and the app will use your CLI credentials

# Server Configuration
PORT=3000
